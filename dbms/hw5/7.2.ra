pi{GroupName, CourseName} (
    pi{GroupId, CourseId} (
        pi{CourseId, StudentId} (
            Marks
        )
        gdiv
        pi{StudentId, GroupId} (
            Students
        )
    )
    nj
    Groups
    nj
    Courses
)
