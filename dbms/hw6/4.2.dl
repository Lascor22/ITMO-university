StudentsWithFall(StudentId, CourseId) :-
    Marks(StudentId, CourseId, Mark),
    Mark < 3.
 
Result(StudentName, CourseName) :- 
    Students(StudentId, StudentName, GroupId),
    Plan(GroupId, CourseId, _),
    Courses(CourseId, CourseName),
    StudentsWithFall(StudentId, CourseId).
