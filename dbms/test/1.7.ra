pi{SessionId}(
    Sessions
)
diff
pi {SessionId}(
    pi{SessionId, Letter}(
        Sessions nj 
        Problems
    ) 
    diff
    pi{SessionId, Letter}(
        Sessions nj 
        Problems nj 
        Runs
    )
)
